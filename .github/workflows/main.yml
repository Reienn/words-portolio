on: [push]

jobs:
  env_setup:
    runs-on: ubuntu-latest
    name: Env setup
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Env setup
        uses: ./
        id: env_setup
        with:
          FIREBASE_CONFIG: ${{ secrets.FIREBASE_CONFIG }}